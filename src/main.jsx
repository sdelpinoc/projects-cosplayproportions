import React from 'react';
import ReactDOM from 'react-dom/client';

import CosplayProp from './CosplayProp';

import './primitive-ui.css';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <CosplayProp />
    </React.StrictMode>,
)
